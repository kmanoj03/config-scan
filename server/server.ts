import { runCli } from './routes/cliRoutes';

runCli();

//Check for failing github actions in a new branch after enabling branch protection rules.
//Enabled the enforcement of the github actions workflow.

//Removed bad configs for this branch as well.